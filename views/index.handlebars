<h1>Burger!</h1>

<h2>Fresh out of the grill</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{burger_name}}

            <button class="eat" data-id="{{id}}" data-eaten="{{devoured}}">Eat? </button>
        {{/unless}}
    {{/each}}
</ul>

<h2>Devoured</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{burger_name}}
        {{/if}}
    {{/each}}
</ul>




<h2>Order a burger</h2>
<form class="create-form">
    <div class="form-group">
        <label for="burgs">Burger Name:</label>
        <input type="text" id="burgs" burger_name="name">
    </div>
    <button type="submit">Order Burger</button>
</form>
